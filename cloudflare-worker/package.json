{
  "name": "kindle-notion-sync-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker webhook relay to trigger Kindle to Notion GitHub Actions sync",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "wrangler": "^3.114.0"
  }
}
